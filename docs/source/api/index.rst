API reference
=============

Reference for every public symbol xpcsjax ships, grouped by submodule. The
top-level page :doc:`public` walks through the seven lazy-loaded names exposed
on :mod:`xpcsjax` itself; the per-submodule pages document the underlying
implementation surface. (:mod:`xpcsjax.gui` is intentionally excluded — it is
a PySide6 desktop application, not a library surface; see
:doc:`/user_guide/gui` for its usage-facing documentation.)

.. toctree::
   :maxdepth: 1

   public
   data
   config
   core
   optimization
   results
   viz
   cli
   runtime
   service
   device
   io
   utils
